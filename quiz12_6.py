class Car:
    def __init__(self, year, model)
        self.speed() = 0
        self.year = year
        self.model= model

    def set_accelerate(self)
        self.speed = self.speed + 5

    def set_brake(self)
        self.speed = self.speed - 5

    def get_speed(self)
        return speed()
